export NODE_ENV=development
export NODE_PATH=/vagrant/server

cd /vagrant/server/applications/account/server
pm2 start index.js --name account


// PRODUCTION
export NODE_ENV=production
export NODE_PATH=/vagrant/server

cd /vagrant/server/applications/account/server


// Start vagrant
cd mue\project\calendar
vagrant up

cd mue\project\calendar
vagrant ssh